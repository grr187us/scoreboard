"""Soccer domain package: state, commands, clocks, rules, formatting, shootout.

Mirrors the top-level ``scoreboard.domain`` package structure for football
(spec section 2.2). Nothing here is imported by any football module; football
is frozen (``.scratch/soccer-mode/IMPLEMENTERS.md`` hard rule 1).
"""

from __future__ import annotations
