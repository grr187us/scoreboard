# Application package boundary

No application code exists yet. Phase 2 will place the Python package here, using the boundaries proposed in `../../docs/PROJECT_STRUCTURE.md`.

The first runtime-skeleton task must prove multi-window startup, clean shutdown, and an offline development launch before adding scoreboard features.
